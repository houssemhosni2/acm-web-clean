export class ExceptionRequestCountEntity {
    /** The count new */
    countNew: number;

    /** The count accepted. */
    countAccepted: number;

    /** The count rejected. */
    countRejected;

    /** The count cancelled. */
    countCancelled: number;

    /** The count closed. */
    countClosed: number;

    constructor() {
    }
}
