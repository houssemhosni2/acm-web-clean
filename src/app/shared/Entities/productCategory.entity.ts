export class ProductCategoryEntity {
  /** The id. */
  id: number;
  /** The code. */
  code: string;
  /** The description. */
  description: string;
  /** The productIdsList. */
  productIdsList: string;
  /** The incentiveRepayment. */
  incentiveRepayment: boolean;
  /** The incentiveRegistration. */
  incentiveRegistration: boolean;
  /** The incentiveOperation. */
  incentiveOperation: boolean;
  /** The incentiveLegal. */
  incentiveLegal: boolean;
}
