
export class CollectionNoteEntity {
/** The id. */
  id : number;

/** The action. */
  action : string;

/** The acm loan id. */
  collectionId : number;

/** The comment. */
 comment : string;

/** The insert by. */
  insertBy : string;

/** The date insertion. */
  dateInsertion :Date;
  constructor() {
  }
}
