export class RelationshipEntity {
    /** The relationship ID. */
    relationshipID: number;

    /** The name. */
    name: string;

    /** The inverse name. */
    inverseName: string;

    /** The directional. */
    directional: number;

    /** The enabled. */
    enabled: boolean;
}
