export class SettingRequiredStepEntity {
  /** The id. */
  id: number;

  /** The product id. */
  productId: number;

  /** The code. -- FIELD VISIT || -- AUDIT REVIEW || -- RISK REVIEW. */
  code: string;

  /** The description. */
  description: string;

  /** The mandatory. */
  mandatory: boolean;
}
