export class RenewalConditionEntity {
    /** The relationship ID. */
    id: number;

    /** The year. */
    year: number;

    /** The minAmount */
    minAmount: number;

    /** The maxAmount. */
    maxAmount: number;

    /** The pourcentage. */
    pourcentage: number;

    /** Ordre */
    ordre: number;
}
