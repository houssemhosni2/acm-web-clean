export class RoleEntity {
    /** The role ID. */
    roleID: number;

    /** The code. */
    code: string;

    /** The description. */
    description: string;

    /** The default item. */
    defaultItem: number;

    /** The enabled. */
    enabled: boolean;

    /** The unique restriction. */
    uniqueRestriction: boolean;

    /** The is group head. */
    isGroupHead: boolean;

}
