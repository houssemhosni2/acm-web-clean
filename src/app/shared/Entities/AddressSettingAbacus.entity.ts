export class AddressSettingAbacusEntity {
  /** The address field. */
  addressField: string;

  /** The label. */
  label: string;

  /** The default text. */
  defaultText: string;

  /** The default address list value ID. */
  defaultAddressListValueID: number;

  /** The customer. */
  customer: boolean;

  /** The use list. */
  useList: boolean;

  /** The address list ID. */
  addressListID: number;

  /** The required. */
  required: boolean;
}
